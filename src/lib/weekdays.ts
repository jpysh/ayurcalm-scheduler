/** Day names in `Date.getDay()` order, as the schedule and time-off records write them. */
export const WEEKDAYS = ['sunday', 'monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday'] as const;
